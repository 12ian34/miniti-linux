# miniti (AUR source) — placeholder

Fill PKGBUILD / miniti-git variant when the app scaffolds.

See ../../docs/distribution.md.

Important: disable Tauri updater signing artifacts for source builds so AUR CI/users are not asked for `TAURI_SIGNING_PRIVATE_KEY`.
